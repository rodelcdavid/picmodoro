import React from "react";

const NotFound = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        flexDirection: "column",
        height: "100vh",
        color: "#000",
        fontSize: "2rem",
      }}
    >
      <h1>404</h1>
      <h3>Not Found</h3>
    </div>
  );
};

export default NotFound;
